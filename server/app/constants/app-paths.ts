export enum AppPaths {
  root = "/",
  api = "/api",
  auth = "/auth",
  login = "/login",
  logout = "/logout",
  user = "/users",
  all = "/all",
  search = "/search",
  google = "/google",
  redirect = "/redirect",
}
